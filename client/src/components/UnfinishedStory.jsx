

export default function UnfinishedStory({title}) {
    return (
        <div>
            <button className="btn"  type="confirm">
                {title}
            </button>
        </div>
    )
};